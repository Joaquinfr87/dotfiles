# ==============================================================================
# SWAY + NOCTALIA — Config principal (Debian 13)
# ==============================================================================
# Basado en:
#   - https://github.com/piratheon/sway-noctalia-dots (estructura modular)
#   - config anterior en ~/repos/dotfiles/sway/config (Gruvbox + hardware)
# Read `man 5 sway` for a complete reference.

# user config directory (modular)
include $HOME/.config/sway/config.d/*
include $HOME/.config/sway/user/*

# system-wide fragments (Debian: 50-systemd-user.conf, etc.)
include /etc/sway/config.d/*
